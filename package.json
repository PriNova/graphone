{
  "name": "graphone",
  "version": "0.1.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix apps/desktop/web run dev --",
    "build": "npm --prefix apps/desktop/web run build",
    "preview": "npm --prefix apps/desktop/web run preview",
    "check": "npm --prefix apps/desktop/web run check",
    "check:watch": "npm --prefix apps/desktop/web run check:watch",
    "check:repo": "node tooling/scripts/check-repository-guardrails.mjs",
    "tauri": "tauri",
    "dev:linux": "tauri dev",
    "dev:windows": "bash -c 'source ~/.cargo/env && cargo tauri dev --target x86_64-pc-windows-msvc --runner cargo-xwin'",
    "build:windows:exe": "bash -c 'source ~/.cargo/env && cargo tauri build --target x86_64-pc-windows-msvc --runner cargo-xwin --no-bundle'",
    "build:windows:portable": "npm run build:windows:exe && bash tooling/scripts/stage-windows-portable.sh",
    "build:windows:debug:exe": "bash -c 'source ~/.cargo/env && cargo tauri build --debug --target x86_64-pc-windows-msvc --runner cargo-xwin --no-bundle'",
    "build:windows:debug:portable": "npm run build:windows:debug:exe && bash tooling/scripts/stage-windows-portable.sh x86_64-pc-windows-msvc debug",
    "build:linux": "tauri build",
    "build:windows": "bash -c 'source ~/.cargo/env && cargo tauri build --target x86_64-pc-windows-msvc --runner cargo-xwin'",
    "build:all": "npm run build:linux && npm run build:windows",
    "run:windows": "bash tooling/scripts/run-windows.sh"
  },
  "license": "MIT",
  "dependencies": {
    "@mariozechner/pi-coding-agent": "^0.52.12",
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.6",
    "@sveltejs/kit": "^2.9.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@tailwindcss/vite": "^4.1.18",
    "@tauri-apps/cli": "^2",
    "clsx": "^2.1.1",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "tailwind-merge": "^3.4.0",
    "tailwindcss": "^4.1.18",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
